// Single source of truth for the site's brand name. Change it here and it
// propagates everywhere it's used (headers, footer, page titles, auth
// pages, static pages) — nowhere else in the frontend should hardcode the
// name directly. The visual logo mark itself lives in
// components/layout/Logo.tsx, which also reads this.
export const SITE_NAME = "EliteCommerce";
