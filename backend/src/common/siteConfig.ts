// Single source of truth for the site's brand name used in transactional
// emails. Mirrors frontend/src/lib/siteConfig.ts — the two are separate
// runtimes so can't share one file, but should be kept in sync.
export const SITE_NAME = 'EliteCommerce';
